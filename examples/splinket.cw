{"start": 0, "end": 1390375, "step": 1.0, "rate": 44100, "channels": 2, "frameSize": 1024, "expr": "# Load the file \"SplinketCommercial7.wav\" globally as the np-array \"splinket\"\n# Shape is (audiosamples, channels)\nload(\"SplinketCommercial7.wav\", \"splinket\")\n\n#print(len(splinket)) # Uncomment to view the length of splinket\n\n# Using integers as audio array indexes, read the stream wobbily\nsplinket_indexer = min(len(splinket)-1, int(x+sin(x/1000)*100))\nsplink = splinket[splinket_indexer,:]\n\n# Apply echo effect\nsplink = delay(splink, [0,5000,10000], [1,0.5,0.25]) #array, timepoints, volumes\n\n# Alter the balance on the left and right channels \nbalance = saw(x/5000)\nmain[0] = splink[:][0] * (0.5-balance*0.5)\nmain[1] = splink[:][1] * (0.5+balance*0.5)"}