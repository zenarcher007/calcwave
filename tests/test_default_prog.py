from calcwave.calcwave import CalcWave

def test_default_prog_no_audiofile():
  # Test that the program runs without an audio file and has the default expression "main = 0"
  pass

def test_default_prog_one_audiofile():
  # Test that the program runs with one audio file loaded and has the default program
  pass

def test_default_prog_two_audiofiles():
  # Test that the program runs with two audio files and has the default program, and that both audio files are loaded in scope
  pass